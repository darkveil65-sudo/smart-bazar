/**
 * Use this logic inside the browser console on the admin page if you need to manually promote a user.
 * 
 * const { doc, updateDoc } = await import('firebase/firestore');
 * const { clientDb } = await import('@smart-bazar/shared/lib/firebase');
 * await updateDoc(doc(clientDb, 'users', 'USER_ID'), { role: 'admin' });
 */

console.log('Admin promotion logic is handled automatically in the Login Page for hapizulsk92@gmail.com');
